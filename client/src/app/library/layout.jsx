export const metadata = {
    title: 'My Library',
    description: 'Manage your reading shelves and track your progress through your personal book collection.',
};

export default function LibraryLayout({ children }) {
    return <>{children}</>;
}
