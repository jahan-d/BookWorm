export const metadata = {
    title: 'Dashboard',
    description: 'View your reading statistics, activity feed, and progress toward your annual reading goals.',
};

export default function DashboardLayout({ children }) {
    return <>{children}</>;
}
