export const metadata = {
    title: 'Create Account',
    description: 'Join the BookWorm community and start tracking your reading habits today.',
};

export default function RegisterLayout({ children }) {
    return <>{children}</>;
}
