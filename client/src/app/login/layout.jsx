export const metadata = {
    title: 'Sign In',
    description: 'Log in to your BookWorm account to continue your reading journey.',
};

export default function LoginLayout({ children }) {
    return <>{children}</>;
}
